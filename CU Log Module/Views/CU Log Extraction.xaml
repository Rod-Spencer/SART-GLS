<UserControl x:Class="Segway.Modules.CU_Log_Module.CU_Log_Extraction"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://www.codeplex.com/CompositeWPF"
             xmlns:shared="http://schemas.actiprosoftware.com/winfx/xaml/shared"
             xmlns:wpfres="clr-namespace:Segway.WPFResources;assembly=WPFResources"
             xmlns:inf="clr-namespace:Segway.Modules.ShellControls;assembly=SegwayShellControls"
             xmlns:plv="clr-namespace:Segway.Modules.Progression_Log_Viewer;assembly=ProgressionLogViewer"
             xmlns:header="http://service.segway.com/schemas/Header/"
             mc:Ignorable="d" x:Name="thisControl"
             d:DesignHeight="500" d:DesignWidth="1000">

    <UserControl.Resources>
        <shared:ConditionalConverter x:Key="ConditionalConverter" />
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Popup x:Name="errorPopup" Grid.Row="1" Grid.RowSpan="2" PopupAnimation="Scroll" MinWidth="400" MinHeight="200" Height="Auto" Width="Auto" AllowsTransparency="False" 
                   PlacementTarget="{Binding ElementName=thisControl}" Placement="Center" IsOpen="{Binding Path=CUSerialNumberValidationErrorPopupOpen}" StaysOpen="True">
            <Border BorderThickness="1"
                    BorderBrush="{DynamicResource {ComponentResourceKey ResourceId=ControlBorderBrush, TypeInTargetAssembly={x:Type wpfres:Brushes}}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Background="{DynamicResource {ComponentResourceKey ResourceId=ToolBar_ItemBrush1, TypeInTargetAssembly={x:Type wpfres:Brushes}}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="35"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Rectangle Grid.ColumnSpan="2" Height="16" Fill="Snow" VerticalAlignment="Top" Opacity="0.5"/>
                        <Image Grid.Column="0" Source="../Images/warning.png" Height="25" Width="25" 
                               HorizontalAlignment="Center" VerticalAlignment="Center" >
                            <Image.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.4"/>
                            </Image.Effect>
                        </Image>
                        <TextBlock Grid.Column="1" Text="Warning" Foreground="Black"
                                   FontSize="16" 
                                   Opacity="0.65" FontFamily="Tahoma" VerticalAlignment="Center" 
                                   HorizontalAlignment="Left" OpacityMask="Black">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Grid>
                    <Border Grid.Row="1" Grid.RowSpan="4" Background="{DynamicResource {ComponentResourceKey ResourceId=LightBackgroundBrush, TypeInTargetAssembly={x:Type wpfres:Brushes}}}"/>
                    <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="{Binding Path=CUSerialNumberValidationErrorMessage}" HorizontalAlignment="Left" Margin="5,5,5,0" FontSize="14.667" FontFamily="Tahoma" Opacity="0.60" VerticalAlignment="Top">
                        <!--<TextBlock.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
                        </TextBlock.Effect>-->
                    </TextBlock>
                    <TextBlock Grid.Row="3" TextWrapping="Wrap" Text="Do you want to continue?" HorizontalAlignment="Center" Margin="5,5,5,0" FontSize="14.667" FontFamily="Tahoma" Opacity="0.60" VerticalAlignment="Top"/>
                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="1" HorizontalAlignment="Right" Width="120" Height="30" Content="Yes" Command="{Binding YesCommand}"
                                Style="{DynamicResource {ComponentResourceKey ResourceId=ButtonStyleOne, TypeInTargetAssembly={x:Type wpfres:Buttons}}}"
                                Margin="0,5,5,10" Visibility="Visible">
                            <Button.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
                            </Button.Effect>
                        </Button>
                        <Button Grid.Column="2" HorizontalAlignment="Right" Width="120" Height="30" Content="No" Command="{Binding NoCommand}"
                                Style="{DynamicResource {ComponentResourceKey ResourceId=ButtonStyleOne, TypeInTargetAssembly={x:Type wpfres:Buttons}}}"
                                Margin="0,5,5,10" Visibility="Visible">
                            <Button.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
                            </Button.Effect>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Popup>

        <Popup PopupAnimation="Scroll" Height="Auto" Width="Auto" AllowsTransparency="True"  
               Placement="Center" PlacementTarget="{Binding ElementName=thisControl}" IsOpen="{Binding Path=Extract_Note_Popup_Open}" StaysOpen="True">
            <Border MinWidth="400" MinHeight="200" BorderThickness="1"  BorderBrush="{DynamicResource {ComponentResourceKey ResourceId=ControlBorderBrush, TypeInTargetAssembly={x:Type wpfres:Brushes}}}" >
                <Grid Background="{DynamicResource {ComponentResourceKey ResourceId=LightBackgroundBrush, TypeInTargetAssembly={x:Type wpfres:Brushes}}}">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Image Source="../Images/NoteIcon.png" Height="45" Width="45" Margin="20,20,0,20"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" >
                            <Image.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.4"/>
                            </Image.Effect>
                        </Image>
                        <TextBlock Text="NOTE" Foreground="Black" FontSize="24" FontWeight="Bold" Margin="10,5,5,5"
                                       FontFamily="Tahoma" VerticalAlignment="Center" HorizontalAlignment="Left" />
                    </StackPanel>
                    <TextBlock Grid.Row="1"
                               Text="{Binding Path=Extraction_Note}" Foreground="Black" FontSize="16" Margin="20,20,5,20"
                               FontFamily="Tahoma" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,0">
                        <Button Content="Cancel" HorizontalAlignment="Right" Width="120" Height="30" Command="{Binding CancelNoteCommand}"
                                    Style="{DynamicResource {ComponentResourceKey ResourceId=ButtonStyleOne, TypeInTargetAssembly={x:Type wpfres:Buttons}}}"
                                    Margin="0,5,5,20" Visibility="Visible" />
                        <Button Content="Continue" HorizontalAlignment="Right" Width="120" Height="30" Command="{Binding ExtractCommand}"
                                    Style="{DynamicResource {ComponentResourceKey ResourceId=ButtonStyleOne, TypeInTargetAssembly={x:Type wpfres:Buttons}}}"
                                    Margin="0,5,5,20" Visibility="Visible"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Popup>


        <Popup Grid.Row="1" Grid.RowSpan="1" Grid.Column="0" Grid.ColumnSpan="1" Width="Auto" Height="Auto"
               IsOpen="{Binding Path=CULogExtract_PopupOpen, Mode=TwoWay}" StaysOpen="False" Placement="Center">
            <Border Width="1200" Height="Auto" Background="{Binding Path=CULogExtract_PopupColor, Mode=TwoWay}" BorderBrush="Black" BorderThickness="2" CornerRadius="10" >
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding Path=CULogExtract_PopupMessage, Mode=TwoWay}" FontSize="16" Margin="40,25" HorizontalAlignment="Center" />
                </ScrollViewer>
            </Border>
        </Popup>


        <header:Header_Bar_Control Grid.Row="0" HeaderText="Extract Event Log" Header_FontSize="16" Header_ImageSource="{Binding Header_Image}" />

        <DockPanel Grid.Row="1" MinHeight="345" DataContext="{Binding}" VerticalAlignment="Stretch" LastChildFill="True" HorizontalAlignment="Stretch">
            <header:Work_Order_Info DockPanel.Dock="Top" Work_Order_Number="{Binding Path=Work_Order_Num, Mode=TwoWay}" 
                                    PT_Serial_Number="{Binding Path=PTSerial,Mode=TwoWay}" 
                                    Work_Order_Color="{Binding Path=WorkOrderColor, Mode=TwoWay}"
                                    />

            <shared:AnimatedExpander DockPanel.Dock="Top" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" 
                                     Width="Auto" Height="Auto"
                                     VerticalAlignment="Stretch" Margin="5" IsExpanded="True"
                                     IsAutoFocusOnExpandEnabled="True" Header="Settings"
                                     IsAutoExpandOnFocusEnabled="False"
                                     IsAutoCollapseOnBlurEnabled="False" Style="{DynamicResource {ComponentResourceKey ResourceId=ExpanderStyleOne, TypeInTargetAssembly={x:Type wpfres:Expanders}}}"
                                     >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!--<Border CornerRadius="3" BorderBrush="#FFA9A9A9" BorderThickness="0.7" Margin="0,5" Grid.Column="0" Grid.ColumnSpan="{Binding Path=BorderColumns, Mode=TwoWay}"/>-->
                        <CheckBox HorizontalAlignment="Left" IsChecked="{Binding Path=IsExtractLog, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TabIndex="1" 
                                     BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}"
                                     BorderThickness="4" Margin="5,0,0,0" VerticalAlignment="Center" Padding="5,0,0,0" Grid.Column="0"
                                     Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:CheckBoxes}, ResourceId=CheckBoxStyleOne}}" >
                            <CheckBox.Content>
                                <TextBlock Text="Extract Log(s)" HorizontalAlignment="Left" Margin="0,5,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" VerticalAlignment="Center">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="4" Opacity="0.2"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </CheckBox.Content>
                            <CheckBox.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                            </CheckBox.Effect>
                        </CheckBox>

                        <CheckBox HorizontalAlignment="Left" IsChecked="{Binding IsClearLog, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TabIndex="1"
                                     BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}"
                                     BorderThickness="4" Margin="25,0,0,0" VerticalAlignment="Center" Padding="5,0,0,0" Grid.Column="1"
                                     Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:CheckBoxes}, ResourceId=CheckBoxStyleOne}}"
                                     Visibility="{Binding Path=Clear_Log_Visibility, Mode=TwoWay}"
                                     >
                            <CheckBox.Content>
                                <TextBlock Text="Clear Log(s)" HorizontalAlignment="Left" Margin="0,5,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" VerticalAlignment="Center">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="4" Opacity="0.2"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </CheckBox.Content>
                            <CheckBox.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                            </CheckBox.Effect>
                        </CheckBox>

                        <CheckBox HorizontalAlignment="Left" IsChecked="{Binding IsFormatLog, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TabIndex="1"  Grid.Column="2"
                                     BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}"
                                     BorderThickness="4" Margin="25,0,0,0" VerticalAlignment="Center" Padding="5,0,0,0" Visibility="{Binding Path=Format_Log_Visibility, Mode=TwoWay}"
                                     Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:CheckBoxes}, ResourceId=CheckBoxStyleOne}}"
                                     >
                            <CheckBox.Content>
                                <TextBlock Text="Format Log(s)" HorizontalAlignment="Left" Margin="0,5,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" VerticalAlignment="Center">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="4" Opacity="0.2"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </CheckBox.Content>
                            <CheckBox.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                            </CheckBox.Effect>
                        </CheckBox>

                        <StackPanel Orientation="Horizontal" Grid.Column="3" HorizontalAlignment="Right" >
                            <CheckBox IsChecked="{Binding Path=IsAutoLoad}" VerticalAlignment="Center" Padding="0,0,5,0" TabIndex="2"
                                      Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:CheckBoxes}, ResourceId=CheckBoxStyleOne}}"
                                      BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}" 
                                      Visibility="{Binding Path=AutoLoad_Visibility, Mode=TwoWay}"
                                      >
                                <CheckBox.Effect>
                                    <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                                </CheckBox.Effect>
                                <CheckBox.Content>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Auto Code Load" HorizontalAlignment="Left" 
                                               Margin="5,5,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" 
                                               VerticalAlignment="Center"
                                               >
                                    </TextBlock>
                                </CheckBox.Content>
                            </CheckBox>
                        </StackPanel>

                    </Grid>
                    <Separator Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" Height="1" Margin="5,0,5,0"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Connectivity:" HorizontalAlignment="Left" Margin="5,5,0,0" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Target CU:" HorizontalAlignment="Left" Margin="0,5,0,0" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <RadioButton Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" IsChecked="{Binding Is_Wired}" 
                                 IsEnabled="True" TabIndex="5"
                                 BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}"
                                 BorderThickness="4" Margin="5,0,0,0" VerticalAlignment="Center" Padding="5,0,0,0"
                                 Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:RadioButtons}, ResourceId=RadioButtonStyleOne}}"
                                 >
                        <RadioButton.Content>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Wired" HorizontalAlignment="Left" Margin="0,10,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" VerticalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="4" Opacity="0.2"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </RadioButton.Content>
                        <RadioButton.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                        </RadioButton.Effect>
                    </RadioButton>
                    <RadioButton Grid.Row="3" Grid.Column="1" HorizontalAlignment="Left" IsChecked="{Binding Is_Wireless}" 
                                 IsEnabled="False" TabIndex="5" Visibility="Hidden"
                                 BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}"
                                 BorderThickness="4" Margin="5,0,0,0" VerticalAlignment="Center" Padding="5,0,0,0"
                                 Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:RadioButtons}, ResourceId=RadioButtonStyleOne}}"
                                 >
                        <RadioButton.Content>
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Wireless" HorizontalAlignment="Left" Margin="0,10,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" VerticalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="4" Opacity="0.2"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </RadioButton.Content>
                        <RadioButton.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                        </RadioButton.Effect>
                    </RadioButton>
                    <CheckBox Grid.Row="3" Grid.Column="2" HorizontalAlignment="Left" IsChecked="{Binding Path=Is_CUA}"
                          Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:CheckBoxes}, ResourceId=CheckBoxStyleOne}}"
                          BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}" 
                          VerticalAlignment="Center" Margin="0" Padding="0,0,10,0"
                          TabIndex="2">
                        <CheckBox.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                        </CheckBox.Effect>
                        <CheckBox.Content>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Rear (A)" HorizontalAlignment="Left" 
                                   Margin="5,10,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" 
                                   VerticalAlignment="Center"
                                   >
                            </TextBlock>
                        </CheckBox.Content>
                    </CheckBox>
                    <CheckBox Grid.Row="3" Grid.Column="3" HorizontalAlignment="Left" IsChecked="{Binding Path=Is_CUB}"
                          Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:CheckBoxes}, ResourceId=CheckBoxStyleOne}}"
                          BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type wpfres:Brushes}, ResourceId=ControlBorderBrush}}" 
                          VerticalAlignment="Center" Margin="20,0,0,0" Padding="0,0,10,0"
                          TabIndex="2">
                        <CheckBox.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.35"/>
                        </CheckBox.Effect>
                        <CheckBox.Content>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Front (B)" HorizontalAlignment="Left" 
                                   Margin="5,10,0,5" FontSize="14.667" FontFamily="Tahoma" Opacity="0.65" 
                                   VerticalAlignment="Center"
                                   >
                            </TextBlock>
                        </CheckBox.Content>
                    </CheckBox>
                </Grid>
            </shared:AnimatedExpander>
            <plv:ProgressionLogViewer Margin="5,0,5,0" Name="ExtractLogViewer" />
            <!--<plv:ProgressionLogViewer.Visibility>
                    <MultiBinding Converter="{StaticResource ConditionalConverter}">
                        <Binding Path="ExtractionInProgress"/>
                        <Binding Source="{x:Static Visibility.Visible}"/>
                        <Binding Source="{x:Static Visibility.Collapsed}"/>
                    </MultiBinding>
                </plv:ProgressionLogViewer.Visibility>-->
            <!--</plv:ProgressionLogViewer>-->
        </DockPanel>
        <Button Grid.Row="2" Width="120" Height="30" Content="Clear" Command="{Binding ClearLogCommand}"
                Style="{DynamicResource {ComponentResourceKey ResourceId=ButtonStyleOne, TypeInTargetAssembly={x:Type wpfres:Buttons}}}"
                Margin="5,5,5,10" HorizontalAlignment="Left" Visibility="{Binding Path=ClearButtonVisibility, Mode=TwoWay}" >
            <Button.Effect>
                <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
            </Button.Effect>
        </Button>

        <StackPanel Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal" Visibility="{Binding Path=NoteVisibility}">
            <TextBlock Text="NOTE" Foreground="Red" FontSize="20" FontWeight="Bold" Margin="10,5,5,10" 
                                       FontFamily="Tahoma" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Path=Extraction_Note}" Foreground="Red" FontSize="16" Margin="10,5,5,10"
                               FontFamily="Tahoma" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        </StackPanel>

        <Button Grid.Row="2" HorizontalAlignment="Right" Width="120" Height="30" Content="Start" Command="{Binding ExtractLogNoteCommand}"
                Style="{DynamicResource {ComponentResourceKey ResourceId=ButtonStyleOne, TypeInTargetAssembly={x:Type wpfres:Buttons}}}"
                Margin="0,5,5,10" Visibility="{Binding Path=ExtractVisibility, Mode=TwoWay}"
                >
            <Button.Effect>
                <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
            </Button.Effect>
        </Button>
        <Button Grid.Row="2" HorizontalAlignment="Right" Width="120" Height="30" Content="Cancel" Command="{Binding CancelCommand}"
                Style="{DynamicResource {ComponentResourceKey ResourceId=ButtonStyleOne, TypeInTargetAssembly={x:Type wpfres:Buttons}}}"
                Margin="0,5,5,10" Visibility="{Binding Path=CancelVisibility, Mode=TwoWay}"
                >
            <Button.Effect>
                <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
            </Button.Effect>
        </Button>

    </Grid>
</UserControl>
