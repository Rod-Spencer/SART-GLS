<UserControl x:Class="Segway.Modules.CU_Log_Module.CU_Log_Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shared="http://schemas.actiprosoftware.com/winfx/xaml/shared"
             xmlns:views="http://schemas.actiprosoftware.com/winfx/xaml/views"
             xmlns:wpfres="clr-namespace:Segway.WPFResources;assembly=WPFResources"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600"
             >

    <UserControl.Resources>
        <shared:StringFormatConverter x:Key="StringFormatConverter" />
        <LinearGradientBrush x:Key="PaletteBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Offset="0.2" Color="#ffffff" />
            <GradientStop Offset="0.5" Color="#f4f4f4" />
            <GradientStop Offset="0.9" Color="#f8f8f8" />
        </LinearGradientBrush>

        <Style x:Key="ListBoxItemStyleTwo" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#444444" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="IsEnabled" Value="{Binding Path=IsDefinitionEnabled}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="column1" MinWidth="100"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition x:Name="row1"/>
                            </Grid.RowDefinitions>
                            <Border x:Name="border" Margin="0.7" Padding="0" Height="Auto" Background="Transparent" BorderBrush="Black" BorderThickness="0" CornerRadius="3" Opacity="0.7">
                                <Grid  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Grid Grid.Column="0" x:Name="gridCollapsed" Visibility="Visible">
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        
                                        <Border Grid.Row="0" Grid.RowSpan="2" BorderBrush="#FFA9A9A9" BorderThickness="1" CornerRadius="3" Margin="0,0,1,1"  Background="Transparent" />

                                        <Image Grid.Row="0" Source="{Binding Path=Image}" Height="50" Margin="6" Width="50" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                                            <Image.Effect>
                                                <DropShadowEffect BlurRadius="4" Opacity="0.5" Color="Snow"/>
                                            </Image.Effect>
                                        </Image>
                                        
                                        <TextBlock Grid.Row="1" x:Name="txtTitle" Text="{Binding Path=Title}" Margin="0,4,0,0" Width="45"
                                                   HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Top" TextWrapping="WrapWithOverflow" Padding="0"
                                                   FontSize="14" FontFamily="Tahoma" >
                                            <TextBlock.Effect>
                                                <DropShadowEffect BlurRadius="4" Opacity="0.5"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                    </Grid>
                                    
                                    <Grid Grid.Column="0" x:Name="gridExpanded" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent" >
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="1*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Row="0" Grid.Column="0" BorderBrush="#FFA9A9A9" BorderThickness="1,0,1,1" CornerRadius="3" Margin="0,0,1,1">
                                            <UserControl Content="{Binding Path=Content}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" />
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                                <Setter TargetName="txtTitle" Property="Opacity" Value="0.65"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="gridCollapsed" Property="Opacity" Value="0.6" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="column1" Property="Width" Value="{Binding ElementName=borderForResizing, Path=ActualWidth}" />
                                <Setter TargetName="row1" Property="Height" Value="{Binding ElementName=borderForResizing, Path=ActualHeight}" />
                                <Setter TargetName="border" Property="Opacity" Value="1.0" />
                                <Setter TargetName="gridCollapsed" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="gridExpanded" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="102"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <!--borderForResizing: We need to have this border here in order to have resizing work properly-->
        <Border Grid.Column="1" BorderThickness="0" HorizontalAlignment="Stretch" Margin="0,0,0,5" VerticalAlignment="Stretch" x:Name="borderForResizing"/>

        <ListBox Grid.Column="0" Grid.ColumnSpan="2" x:Name="listBox" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalAlignment="Stretch" 
                 ScrollViewer.VerticalScrollBarVisibility="Disabled" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                 ItemContainerStyle="{DynamicResource ListBoxItemStyleTwo}" 
                 SelectedIndex="{Binding Path=SelectedPanel, Mode=TwoWay}" 
                 ItemsSource="{Binding Path=WindowList, Mode=TwoWay}"
                 Background="Transparent"
                 BorderThickness="0"
                 >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <views:AnimatedWrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>
